const main =' http://localhost:8999/'

const auth: string = 'api/auth'
const basket: string = 'api/basket'
const favorites: string = 'api/favorites'
const popular: string = 'api/popular'
const catalog: string = 'api/catalog'
const orders: string = 'api/orders'

export const AUTH: string = main + auth
export const BASKET: string = main + basket
export const FAVORITES: string = main + favorites
export const POPULAR: string = main + popular
export const CATALOG: string = main + catalog
export const ORDERS: string = main + orders
