export type Tooltip = {
    show: boolean
}

export type ErrorAlert = {
    show: boolean,
    text: string,
    url: string
}

export type Alert = {
    text: string,
    visible: boolean,
    focus: boolean,
    stoped: boolean,
}